name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: BijanProgrammerWebsites/hub/.github/workflows/deploy-nextjs.yml@master
    with:
      domain: slides.bijanprogrammer.com
      base-href: /auth/
      process-name: slides.bijanprogrammer.com_auth
      port: 3012
    secrets: inherit
